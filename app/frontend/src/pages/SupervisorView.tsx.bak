import { Container, Grid2, Card, CardContent, CardHeader, Box, Typography, List, ListItem, ListItemIcon, ListItemText, Chip, Toolbar, Button } from '@mui/material'
import { Warning, TrendingUp, People, CheckCircle, AccessTime } from '@mui/icons-material'
import StatCard from '../components/StatCard'
import BurnoutChart from '../components/BurnoutChart'

export default function SupervisorView() {
  return (
    <Box sx={{ flex: 1, overflow: 'auto', bgcolor: 'background.default', pt: { xs: 8, sm: 0 } }}>
      {/* Header */}
      <Toolbar sx={{ bgcolor: 'background.paper', boxShadow: 1, mb: 3 }}>
        <Box sx={{ flex: 1 }}>
          <Typography variant="h4" sx={{ fontWeight: 700 }}>
            Dashboard
          </Typography>
          <Typography variant="caption" color="textSecondary">
            Team Overview & Real-time Analytics
          </Typography>
        </Box>
        <Box sx={{ textAlign: 'right' }}>
          <Typography variant="caption" color="textSecondary">
            Last updated
          </Typography>
          <Typography variant="body2" sx={{ fontWeight: 600 }}>
            Just now
          </Typography>
        </Box>
      </Toolbar>

      <Container maxWidth="lg" sx={{ pb: 4 }}>
        {/* KPI Cards Grid */}
        <Grid2 container spacing={2} sx={{ mb: 3 }}>
          <Grid2 xs={12} sm={6} md={3}>
            <StatCard
              title="Team Members"
              value={24}
              icon={<People />}
              bgColor="primary"
              trend={5}
            />
          </Grid2>
          <Grid2 xs={12} sm={6} md={3}>
            <StatCard
              title="High Risk"
              value={3}
              icon={<Warning sx={{ color: 'error.main' }} />}
              bgColor="error"
              trend={-2}
            />
          </Grid2>
          <Grid2 xs={12} sm={6} md={3}>
            <StatCard
              title="Medium Risk"
              value={5}
              icon={<TrendingUp sx={{ color: 'warning.main' }} />}
              bgColor="warning"
              trend={1}
            />
          </Grid2>
          <Grid2 xs={12} sm={6} md={3}>
            <StatCard
              title="Avg Wellbeing"
              value={7.8}
              icon={<CheckCircle sx={{ color: 'success.main' }} />}
              bgColor="success"
              isScore
              trend={3}
            />
          </Grid2>
        </Grid2>

        {/* Charts & Analytics */}
        <Grid2 container spacing={3}>
          <Grid2 xs={12} md={8}>
            <Card>
              <CardHeader title="Burnout Trend" subheader="Last 30 days" />
              <CardContent>
                <BurnoutChart />
              </CardContent>
            </Card>
          </Grid2>
          <Grid2 xs={12} md={4}>
            <Card>
              <CardHeader title="At Risk" />
              <CardContent>
                <Typography>John Smith - High Risk</Typography>
                <Typography>Sarah Johnson - Medium Risk</Typography>
              </CardContent>
            </Card>
          </Grid2>
        </Grid2>
      </Container>
    </Box>
  )
}